package com.group23.model;

import jakarta.persistence.*;

/**
 * Represents an open-ended question where users can provide any text as an answer.
 */
@Entity
public class OpenEndedQuestion extends Question {
    // No additional fields

    // Getters and setters
}
